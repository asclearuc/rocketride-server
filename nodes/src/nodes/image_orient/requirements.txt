# headless: this node calls core, imgproc, imgcodecs and objdetect (FaceDetectorYN),
# never a GUI function and nothing from contrib.
opencv-python-headless<5
#
# Nothing else is named on purpose.
#
# numpy is a base engine dependency, so beyond cv2 the detector costs nothing but the
# model file itself. That is the whole reason YuNet was chosen over MediaPipe BlazeFace,
# which would have pulled in mediapipe.
#
