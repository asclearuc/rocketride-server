# contract-check: skip-install  reason: opt-in OCR engine on top of the project's torch baseline. PR lane skips; nightly --install-all installs and verifies. Was `disable` on the premise that the install could never succeed; measured false — it installs cleanly at surya-ocr 0.16.1. The engine resolves 0.16.1, not the 0.17 this loader targets, and the cause is transformers, not opencv: surya-ocr>=0.17 needs transformers>=4.56.1 (>=0.21 needs 5.12.1) against the tree's pinned transformers==4.53.3. The honest >=0.17,<0.18 range is scope item 6 and costs a transformers move. The `# contract-check: ignore` markers on the surya.* imports in surya.py were paired with `disable` and stay for now — revisiting them belongs to item 6, which moves the version they are written against.
# Requirements for Surya OCR loader
# Direct imports only

surya-ocr
Pillow
