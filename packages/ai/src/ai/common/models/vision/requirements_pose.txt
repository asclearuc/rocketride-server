# contract-check: skip-install  reason: rtmlib + onnxruntime-gpu pull a large tree; PR lane skips, nightly --install-all verifies.
# Pose-specific deps — NOT transformers; this loader is non-torch. The per-platform wheel
# choice for the shared runtime is pkg_families' business now. numpy is a base engine dep.
rtmlib>=0.0.13
Pillow
